<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Gather Music</title>
</head>
<body>
    <nav>
        <% if (user) { %>
          <a href="/">Home</a>
          <a href="/movies" <%- title === 'All Movies' ? 'class="active"' : '' %>>View Events</a>
          <a href="/performers/new"	<%- title === 'Add Performer' ? 'class="active"' : '' %>>Add an Artist</a>
          <a href="/movies/new" <%- title === 'Add Movie' ? 'class="active"' : '' %>>Artist Page</a>
          <a href="/logout">Log Out</a>
        <% } else { %>
          <a href="/auth/google" class="login">Log In&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
        <% } %>
      </nav>